name: CI

on:
  pull_request:
    branches:
      - dev
      - main
      - backend-optimization

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: trust_center_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d trust_center_ci"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    env:
      DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/trust_center_ci
      NODE_ENV: test
      ALLOW_SEED_FALLBACK: false

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Check formatting
        run: bun run format:check

      - name: Check for line ending issues
        run: git diff --check

      - name: Apply schema
        run: bun run db:cleanapply

      - name: Seed database
        run: bun run db:seed

      - name: Run tests
        run: bun run test

      - name: Build project
        run: bun run build
