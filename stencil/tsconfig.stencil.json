/* ================================
  TL;DR  -->  🧠 The core reason why we need a separate tsconfig for stencil
        React + Vite:
        - Compile TS → JS for one app
        - Output is JS that runs in the browser
        Stencil:
        - Compiles TSX → Web Components
        - Generates multiple artifacts:
            - Custom elements
            Type definitions (components.d.ts)
            React proxies
            Optional loaders
            Needs different compiler assumptions
        - Because of that, Stencil cannot share the same tsconfig.json as your React app.
================================ */

{
  "compilerOptions": {
    "module": "esnext",
    "declaration": true,
    "noImplicitAny": false,
    "removeComments": true,
    "noLib": false,
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "target": "es2017",
    "sourceMap": true,
    "lib": ["es6"]
  },
  "include": ["src"],
  "exclude": ["node_modules", "**/*.spec.ts", "**/__tests__/**"]
}
