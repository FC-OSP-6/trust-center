/* ================================
  TL;DR  -->  connection to backend graphQL route

      - build api query that could request needed data to fill
      each of the sections depending on which page we're on
================================ */
// REVIEW: This file is currently a stub while `src/api.ts` contains the active implementation; consider removing/merging to avoid parallel API entry points.
