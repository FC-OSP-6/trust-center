/* ================================
  TL;DR  -->  shared 'contracts'

  - single source of truth for control/faq/resource shapes
  - shared pagination types for graphql + ui
  - prevents FE / BE schema drift
================================ */
