/* ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  TL;DR  -->  api unit tests placeholder

  - prevents vitest "no test suite found" failure while api unit tests are pending
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ */

import { describe, it } from 'vitest';

describe.skip('api unit tests', () => {
  it('placeholder', () => {
    // intentionally skipped until api unit tests are added
  });
});
