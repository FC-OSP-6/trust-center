/* ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  TL;DR  -->  graphql integration tests (placeholder)

  - tests will be added after server/e2e smoke is stable
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ */

import { describe, it } from 'vitest';

describe.skip('graphql integration', () => {
  it('placeholder', () => {
    // intentionally skipped until graphql test fixtures/mocks are ready
  });
});
